apiVersion: v1
kind: Service
metadata:
  name: ccnp-webhook
  namespace: ccnp
spec:
  ports:
    - name: https
      port: 9443
      protocol: TCP
  selector:
    # IMPORTANT:
    # this has to match the selector in our Deployment later
    app: ccnp-webhook
